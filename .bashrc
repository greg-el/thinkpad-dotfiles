#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias aur='. ~/scripts/aur-auto'
alias theme='. ~/scripts/theme'
alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '


